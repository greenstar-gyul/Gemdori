<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gemdori.community.mapper.TopicMapper">

    <insert id="insertTopic" parameterType="com.gemdori.community.vo.TopicVO">
        INSERT INTO gemdori_topic_tbl (TOPIC_CODE,
										USER_CODE,
										GAME_CODE,
										TOPIC_TITLE,
										TOPIC_CONTENTS,
										WRITE_DATE)
        VALUES ('T' || TOPIC_SEQ.nextval,
                #{userCode},
                #{gameCode},
                 #{topicTitle},
                  #{topicContents},
                  sysDate)
    </insert>
    <!-- 게시글 목록 조회 -->
    <select id="selectTopicList" resultType="com.gemdori.community.vo.TopicVO">
        SELECT 
            TOPIC_CODE,
            USER_CODE,
            GAME_CODE,
            TOPIC_TITLE,
            TOPIC_CONTENTS,
            WRITE_DATE
        FROM gemdori_topic_tbl
    </select>
    <!-- 게시글 상세 조회 -->
<select id="selectTopicByCode" parameterType="String" resultType="com.gemdori.community.vo.TopicVO">
    SELECT 
        TOPIC_CODE,
        USER_CODE,
        GAME_CODE,
        TOPIC_TITLE,
        TOPIC_CONTENTS,
        WRITE_DATE
    FROM gemdori_topic_tbl
    WHERE TOPIC_CODE = #{topicCode}
</select>
    
</mapper>
