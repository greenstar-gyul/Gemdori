<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gemdori.community.mapper.ReplyMapper">

    <insert id="insertReply" parameterType="com.gemdori.community.vo.ReplyVO">
        INSERT INTO gemdori_comments_tbl (COMMENT_CODE, 
                             TOPIC_CODE, 
                             USER_CODE, 
                             COMMENT_CONTENTS, 
                             WRITE_DATE)
        VALUES ('O' || gemdori_comment_seq.nextval,
                #{topicCode}, 
                #{userCode}, 
                #{commentContents}, 
                sysdate)
    </insert>

    <select id="selectRepliesByTopicCode" parameterType="String" resultType="com.gemdori.community.vo.ReplyVO">
        SELECT * FROM gemdori_comments_tbl WHERE TOPIC_CODE = #{topicCode} ORDER BY WRITE_DATE DESC
    </select>

</mapper>
