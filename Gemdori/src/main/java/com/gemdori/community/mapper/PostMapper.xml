<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gemdori.community.mapper.PostMapper">

    <insert id="insertPost" parameterType="com.gemdori.community.model.PostVO">
        INSERT INTO gemdori_topic_tbl (
            TOPIC_CODE, 
            USER_CODE,
			GAME_CODE,
			TOPIC_TITLE,
			TOPIC_CONTENTS,
			WRITE_DATE
        ) VALUES (
            #{topicCode}, 
            #{userCode}, 
            #{gameCode}, 
            #{topicTitle}, 
            #{topicContents},
            #{writeDate}
        )
    </insert>

</mapper>
